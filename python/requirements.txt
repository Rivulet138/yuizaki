# Yuizaki default runtime dependencies.
# Release/install scripts use the platform lock files in this directory.
# Optional model backends such as FunASR/SenseVoice and SoulX-Singer-SVC are
# not installed by default because their native dependency stacks
# are version-, CUDA-, and Python-build-sensitive. The default runtime calls
# those capabilities through lightweight HTTP clients.

# Web/API runtime
fastapi>=0.136,<1
pydantic>=2.12,<3
uvicorn[standard]>=0.49,<1
httpx>=0.28,<1
aiofiles>=25,<26
python-multipart>=0.0.32,<1
python-socketio>=5.16,<6

# Database
sqlalchemy>=2.0.50,<3
alembic>=1.18,<2

# Numeric and media foundations
numpy>=2.1,<3
Pillow>=12,<13

# ASR/OCR/TTS capabilities that are compatible with the current default stack
sherpa-onnx>=1.13.2,<2
rapidocr-onnxruntime>=1.2.3,<2
genie-tts==2.0.2
huggingface-hub>=0.36,<1

# Vector database and embeddings
qdrant-client>=1.18,<2
sentence-transformers>=2.7,<3
tiktoken>=0.13,<1
