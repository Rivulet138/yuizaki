# 仓库评估

评估日期：2026-07-19。范围包括 Electron、Vue、Python、Node MCP、SoulX 服务、Windows/Linux 启动链、测试、依赖、磁盘资源和维护文档。

## 结论

Yuizaki 已具备 AI 桌宠 Agent 的完整骨架：桌宠渲染、实时语音、实时视觉、记忆、工具、插件、资源下载、延迟指标和双平台启动链已经形成。当前主要问题不再是“缺少功能”，而是发布工程与资源供应链还没有达到可稳定分发的标准。

综合成熟度：开发预览可用，个人长期使用基本可行，不建议在修完 P0 前公开分发完整资源包。

## 本次已修复

- 默认记忆统一为 SQLite，不再由安装模板降级为内存。
- 默认长期记忆数据库加入备份与恢复目标。
- 前后端 LLM 默认预算统一为 `131072/8192`。
- Qdrant 从可变 `latest` 固定到 `v1.18.3`。
- 删除无效 ASR/OCR 旧环境字段。
- Electron 更新到 42 系最新补丁，Node 类型定义对齐内置 Node 24。
- 新增 Dependabot、文档契约检查、依赖与资源管理文档。

## P0：发布前必须完成

1. **模型资源锁与校验**：Sherpa 直链归档缺少 SHA-256；SoulX 和部分 Hugging Face 下载默认跟随移动分支或默认 revision。任何被劫持或被上游替换的资源都可能直接进入本地执行链。
2. **Python 可复现锁**：当前只有版本范围，没有平台锁和哈希。应按 Windows/Linux、CPU/CUDA 生成可验证锁文件。
3. **素材授权清单**：仓库内存在 Live2D 模型、字体、角色图片和模型卡。必须逐项记录来源、许可证、可否修改、可否再分发。
4. **自定义数据备份策略**：默认 `memory.db` 已覆盖，但仓库外自定义数据库路径仍需外部备份或受控注册。

## P1：下一阶段

1. 资源面板提供体积、版本、hash、许可证、最后使用时间和永久卸载。
2. 将 Python 动态 `pip install` 改为安装阶段显式依赖，运行进程不再自行改变环境。
3. 新增产品评测集：首 token、首句、首音频、打断 P50/P95、视觉事实准确率、记忆错误率、工具成功率。
4. Node 24 升为 CI 主基线，Node 22 作为短期兼容；增加 Python 新版本试验矩阵。
5. 将资源下载和模型加载失败分类为网络、磁盘、校验、许可、运行时不兼容，避免只返回通用错误。
6. 跟踪 `@vue/test-utils` 间接依赖的过时 `glob@10.5.0`；它仅在开发测试链中出现，当前审计无漏洞。

## P2：工程优化

- 对 Electron 主版本升级建立季度迁移窗口。
- 生成 npm、Python、Docker 和模型资源 SBOM。
- 将大型内置角色资源移出源码仓库或使用明确的受权制品渠道。
- 增加性能预算，防止实时视觉、ASR partial 和 TTS 队列共同造成内存增长。

## 不建议做的事

- 不要把 OCR 当作实时视觉主链；OCR 只适合精确读字。
- 不要默认下载全部模型；按需下载更符合桌宠首次使用体验和磁盘预算。
- 不要自动清空整个 Hugging Face 或模型缓存；需要按资源 ID 和依赖关系永久卸载。
- 不要为了兼容历史配置继续保留无效字段；当前项目应维持单一配置契约。
- 不要在未确认素材授权前公开仓库或发布完整安装包。

## 验证口径

每次发布至少执行：Electron 类型检查、lint、单元测试、构建、渲染包审计；Python Ruff 关键错误检查、Pyright、pytest；Node MCP 测试；Windows/Linux 启动检查；文档链接与配置契约检查；npm audit 与 pip check。

架构事实见 [ARCHITECTURE.md](ARCHITECTURE.md)，具体依赖与资源策略见 [DEPENDENCIES.md](DEPENDENCIES.md) 和 [RESOURCE_MANAGEMENT.md](RESOURCE_MANAGEMENT.md)。
