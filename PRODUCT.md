# Yuizaki 产品目标

更新日期：2026-07-19。

## 定位

Yuizaki 是 AI 桌宠 Agent：它常驻桌面，以角色、关系、语音、视觉和行动形成连续陪伴。控制面板是辅助入口，不是产品主角，也不扩展成通用 AI 工作台。

## 核心体验

1. **随时可说**：按住鼠标侧键或自定义快捷键说话，释放后快速回应，可随时打断。
2. **真的看见**：桌宠按需理解当前屏幕画面；OCR 只负责精确读字。
3. **记住关系**：长期记忆表现为称呼、偏好、事件、承诺和禁忌，而不是数据库表格。
4. **能够行动**：技能和插件让桌宠读取日程、控制媒体、使用浏览器或调用本地工具，所有高影响动作经过权限确认。
5. **像角色而非接口**：Live2D/VRM 动作、语音、表情和微状态共同表达反馈，不使用状态泡泡。

## 首屏与面板

桌宠层负责视觉存在与直接互动。面板主要入口为陪伴、对话、她的记忆和技能；模型、MCP、提示词、执行记录与底层参数属于高级设置。UI 文本只保留字段名、状态、功能描述和必要错误，不展示教学式解释。

## 运行档位

- 即时陪伴：最低交互延迟，适合问候与短对话。
- 本地均衡：流式 ASR、快速文本模型和句段 TTS。
- 深度任务：强推理、视觉与工具链，允许更高延迟。

是否启用 thinking 由场景与模型能力决定；不对 DeepSeek 日常对话做全局强制关闭。

## 数据原则

- 默认 SQLite 持久记忆，删除是永久删除。
- 实时视觉只保留会话最新帧于内存，默认不建立截图历史。
- 模型按首次使用或用户手动选择下载。
- 用户应能查看资源体积、来源、版本、许可证和永久卸载影响。
- 自定义数据路径必须明确备份责任。

## 成功指标

- `speech_end -> playback_start` 的 P50/P95
- 首 token、首稳定句、首 TTS chunk 延迟
- 打断确认与停止播放成功率
- 屏幕事实准确率和错误“看见”率
- 长期记忆召回准确率与错误记忆率
- 工具成功率、权限拒绝率和单轮成本
- 安装成功率、首次可对话时间与模型下载失败率

## 当前阶段

核心交互链已形成，下一阶段优先级是资源供应链、Python 可复现安装、素材授权、跨平台真实设备验证和体验评测集，而不是继续增加面板数量。详见 [REPOSITORY_AUDIT.md](REPOSITORY_AUDIT.md)。
