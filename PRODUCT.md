# Product

## 产品定位

Yuizaki 是一个面向 Windows 和 Linux 桌面的本地 AI 桌宠 agent。它服务于希望桌面角色能够长期陪伴、自然对话、记住偏好，并在必要时安全调用本地能力的个人用户与开发者。

## 用户

- 想要一个可长期使用的桌面 AI 伙伴，而不是一次性聊天页面的用户。
- 需要本地模型、TTS、ASR、Live2D/VRM、MCP 和插件能力来扩展桌宠 agent 的开发者。
- 希望桌宠能把记忆、语音、实时屏幕视觉、本地资源和工具执行自然串联起来的重度使用者。

## 核心价值

- 桌宠 agent 对话入口清晰，默认中文可直接使用。
- 桌宠不是装饰层，而是能接收 LLM 输出的表情、动作和语音联动。
- TTS/ASR/实时屏幕视觉/SVC 作为可配置能力接入，OCR 只作为需要精确读字时的备用工具，不强迫用户一次性安装全部重型模型栈。
- 记忆默认轻量可用，Qdrant 作为可选持久向量检索增强。
- 设置面板承担必要的运行时配置、模型检测、资源预取和连接测试，但不成为主体验。

## 设计原则

- 陪伴优先: 首屏和主要流程服务于聊天、人物、记忆和语音联动。
- 渐进复杂度: 高级工具、MCP、插件、计划任务和调试能力只作为桌宠 agent 的支撑能力，放在二级入口。
- 本地状态可感知: 连接、模型、语音、记忆和桌宠状态要能被用户快速判断。
- 视觉优先看画面: 桌宠应优先接收实时屏幕帧来理解桌面变化，只有在用户明确需要读取文字时才调用 OCR。
- 资源边界清晰: 模型、音频、数据库和导入的 Live2D/VRM 不与临时日志混在一起。
- 失败可解释: 外部 LLM、ASR、TTS、SVC、Qdrant 不可用时，应明确显示能力降级，而不是让桌面应用整体失败。

## 实时视觉契约

- 主路径是“桌面画面采样 -> Socket.IO 短时帧缓存 -> 多模态模型理解 -> 下一轮对话使用”，不是先 OCR 再把文字交给模型。
- 用户必须能在桌宠主页看到视觉是否启用、当前显示器、采样间隔、最近一次后端确认和失败原因。
- 画面只保留短时上下文，不写入聊天记录和长期记忆；超过有效期后不得继续引用。
- 模型不支持图像、画面缺失、画面过期或内容不可辨认时，桌宠必须明确表示无法确认，不得猜测屏幕内容。
- OCR 是显式备用能力，只用于逐字读取、票据或扫描文档等任务，不得作为实时视觉的默认处理链路。
- 切换显示器、关闭视觉或隐藏主面板时，采集调度应立即响应，避免静默持续采集。
- 视觉证据必须区分当前捕获 `frame_id` 与实际完成分析的 `observation_frame_id`。复用旧结论时标记为 `cached` 并保留观察时间；客户端上传的 caption 只能作为未验证提示，不能冒充视觉模型结论。

## Prompt 契约

- 核心身份、证据边界、视觉真实性、工具完成态、权限和注入防护由后端固定注入，不能被角色卡、世界书或用户自定义 Prompt 覆盖。
- 任务模式与日常陪伴模式只调节表达和协作方式，不改变安全边界与事实标准。
- 角色卡描述稳定人格，世界书提供背景事实；两者都不能把外部文本升级为系统指令。
- 后端按固定顺序编译 Prompt：核心策略、模式策略、响应策略、桌宠动作输出契约、用户配置、召回证据、运行时派生信号、原始会话。每个编译块必须携带稳定的 `id/source/trust/authority/order`，同优先级按 `id` 排序，禁止由调用路径临时前置同名块。
- 角色卡、世界书、工作区 Prompt、会话摘要、长期记忆、关系事件、视觉内容和工具结果必须标记来源并作为无指令权的数据注入；工具结果使用结构化字段保留 `source/trust/success`，不得把插件或页面文本直接拼成高权限自然语言指令。
- 单帧视觉 Prompt 必须明确禁止推断前后变化、隐藏状态或操作完成态；缓存观察要同时暴露当前帧年龄与原观察年龄，避免模型把旧结论描述成刚刚看到的事实。
- Prompt 默认值必须带版本迁移，升级后保留用户真正修改过的内容，同时替换未改动的旧默认模板。
- UI 展示的 Prompt 字段必须实际进入对话请求，并由后端测试验证最终 system message 的组装顺序和内容。
- 模型生成的桌宠动作只接受严格 JSON Schema 和后端白名单过滤后的 snake_case 字段；旧 camelCase 只在输入兼容层读取，向前端输出时必须归一化。动作 envelope 与每个 action 携带 schema 版本和来源，但保持动作 `payload` 的消费形状稳定。

## 模型能力契约

- 模型能力必须区分“官方资料登记”“模型名推断”和“未知”；只有带来源与核验日期的登记项才能展示上下文窗口、最大输出、生命周期和参考价格。
- 价格是按核验日期保存的参考快照，不是实时账单依据；提供商调整价格或模型生命周期后必须更新登记项，未知模型不得套用同系列价格。
- 已明确登记为纯文本的模型不得接收屏幕图片；未配置独立视觉模型时，只能注入“视觉不可用”证据并禁止声称已看到画面。未知本地或自定义模型保留兼容路径，但必须继续遵守视觉真实性提示。
- 设置页发现上下文或最大输出超过登记上限时应提示用户，不静默篡改配置；已弃用模型必须显示替代模型和提供商公布的停用时间。

## 长期记忆契约

- 长期记忆按画像、工作、事件、关系、反思和稳定事实分层；持久化记录不再使用 active、archived、superseded 或 deleted 等软删除状态字段。
- 召回内容是可能过期或出错的辅助证据。用户当前明确陈述优先于旧记忆；冲突项必须由用户确认后更新或永久删除，不得静默拼接成新事实。
- 自动维护只生成过期工作记忆、明确低质量条目和完全重复项的候选预览，不得自动删除画像、关系、高重要度和显式保护项。
- 清理必须先预览，说明扫描范围、原因和动作；执行时要求独立确认，并从权威存储与检索索引中永久删除选中记录，不提供恢复入口。
- 长期记忆首屏只展示关系记忆、写入、整理与永久删除；原始文档写入、检索轨迹、后端过滤和低层查询属于显式开启的高级工具，不得默认占据陪伴入口。
- 用户侧单条与批量操作统一称为“永久删除”；重复删除是幂等操作，只报告本次实际删除的条目。
- 记忆写入、修改和永久删除要记录时间、原因与独立审计动作，但删除审计不得保留被删除的记忆正文。没有后端成功回执时，桌宠不得声称已经记住或删除。

## 场景与技能契约

- 场景默认模型必须真正进入该场景的每次对话请求；用户为单轮显式选择模型时，单轮选择优先且不得污染其他场景。
- 场景模型必须从当前 LLM 提供商的模型目录中选择，同时允许用户录入自定义模型 ID；目录请求失败时保留当前值与手动录入能力，不把网络失败误报为配置丢失。
- 场景设置必须从常驻侧栏可达；工具和 MCP 服务使用运行时目录选择器，不要求用户手写 JSON 或服务 ID。已保存但失效的能力必须标为不可用并允许用户清除。
- 工具限制与 MCP 服务限制必须在 Agent 工具暴露阶段执行。MCP 限制只能开放对应服务的工具，不能仅保存字段或依赖 Prompt 提醒模型自律。
- 插件只展示已经接入运行时的工具、桌宠事件、模型能力和权限。插件清单只接受当前 manifest 字段；未知字段直接拒绝加载，不得作为迁移输入或向渲染层发布虚假入口。
- 插件脚本必须运行在独立受限进程中；当前运行时启用 Node Permission Model，仅允许子进程读取选中的入口文件，网络、文件写入、命令、子进程和 Agent 调用继续由主进程权限代理。`node:vm` 只用于上下文封装，不得被描述为安全边界。
- 插件运行时快照必须发布真实的 `executionIsolation` 字段，技能面板只据此显示“受限进程”，不得用静态文案冒充已启用隔离。
- 桌宠主导航的共享与渲染类型使用 `Navigation*` 命名，不再以 `Workbench*` 作为产品模块契约；内部传输客户端或迁移测试中的旧目录名不得重新形成远程 UI 注入面。
- 应用启动、工作区监听、快捷键和桌宠运行时编排只允许由 `AppShell` 持有一个实例；面板与全局弹窗通过窄作用域运行时桥接调用，不得重复启动连接、注册 watcher 或初始化桌宠。
- 窄窗口下侧栏可以收敛为图标导航，但所有隐藏文字的图标按钮必须保留稳定的 `aria-label` 与 `title`；核心面板至少在 430px 宽度下可导航、可滚动且不得横向裁切。
- Live2D 与 VRM 渲染依赖必须按当前模型类型分路加载；Live2D 启动不得预加载 Three/VRM 运行时。控制面板入口不得预加载 Live2D、Pixi 或 Three；多入口构建必须使用入口感知分包，构建后执行渲染器包审计。Element Plus 组件、样式和图标只按实际使用项导入，不得在入口安装完整插件、全量主题或通配注册完整图标包，并由构建审计限制对应资源体积。
- 连接测试必须读取响应体的 `ok` 结果；HTTP 请求成功但能力测试失败时，UI 必须显示失败。

## Electron 安全契约

- 主窗口与桌宠窗口必须显式启用 Chromium sandbox，并保持 `nodeIntegration=false`、`contextIsolation=true`；预加载只暴露窄接口。
- 高权限 IPC 必须同时校验发送者和输入。生产环境只信任打包目录中的 `index.html` 与 `pet-window.html`，不得把任意 `file://` 页面视为可信渲染器；开发环境只信任配置的开发服务器同源页面。
- 所有窗口导航和新窗口请求默认拒绝非可信目标。生产控制面板与桌宠窗口必须通过标准、secure、受 CSP 约束的 `yuizaki-app://renderer/` 只读协议加载；协议处理器只接受 GET/HEAD，并拒绝错误主机、路径穿越和打包目录外资源。用户显式导入的桌宠模型资源可继续按资源权限使用 `file:`，但页面本身不得退回 `file://`。
- 预加载事件订阅必须保存包装后的监听器引用并可精确注销；重复订阅同一回调不得累计监听器。

## 运行资源契约

- 进程内存、会话缓存、模型文件、长期记忆和用户配置是不同资源，UI 不得用一个含糊的“内存清理”按钮混合处理。
- 资源按钮必须直接命名为“清理 HTTP 缓存”；该动作仅清 Electron 会话 HTTP 缓存，不删除登录状态、工作区、模型、插件、聊天或长期记忆，也不承诺强制释放全部 RAM。
- 资源面板展示真实采样值、采样时间和进程范围；浏览器模式无法读取 Electron 进程时应明确降级。
- OCR 模型只在首次显式精确读字时初始化，模型加载与推理不得阻塞主事件循环；实时视觉观察不得触发 OCR 初始化。
- 运行日志必须滚动保存并设置文件数量上限，模型、记忆、用户配置和导入的桌宠资源不得被日志清理流程删除。
- 离线 SenseVoice 与流式 Zipformer2 CTC 必须使用独立目录和资源状态；流式模型只有在真实创建 `OnlineRecognizer` 与识别流成功后才能标记为已验证。
- SoulX、Sherpa 离线/流式、Genie TTS、长期记忆嵌入等可选模型不得随源码安装。用户首次启用对应服务时自动准备缺失资源，也可在资源页勾选后手动下载；并发请求必须去重，已就绪资源必须跳过。

## 即时语音契约

- 即时档在官方 OpenAI Realtime 凭据可用时使用 Electron 渲染端 WebRTC 原生语音到语音链路；后端只签发短期 `ek_` 客户端密钥，长期 API Key 不得进入渲染进程、日志、会话记录或诊断指标。单个连接最多复用 55 分钟，下一次语音输入前自动轮换，避开供应商会话时限。
- 鼠标侧键按下时启用 Realtime 麦克风音轨，松开后手动提交输入音频并创建响应；短于 120ms 的误触只清空输入缓冲，不触发模型响应。
- 即时语音是低延迟陪伴通道，不直接执行本地工具、MCP、屏幕读取、文件操作或长期记忆写入。需要精确证据或执行能力时，应切换到均衡或深度档的 Agent 流水线。
- Realtime 连接、鉴权、麦克风或 WebRTC 协商失败时，本轮自动回退现有流式 ASR、文本 Agent、句段 TTS 链路；均衡和深度档不得被 Realtime 配置影响。
- Realtime 用户与助手转写必须按一轮事务原子写入当前工作区会话；输入转写仅作为会话记录参考，不得宣称与语音模型实际听取内容完全一致。
- 中断必须同时发送 `response.cancel` 与 `output_audio_buffer.clear`，并立即停止本地播放状态；渲染端只记录连接、首响应、首播和中断确认耗时，不记录音频或转写正文。
- Realtime 远端音轨只在渲染进程内计算限频后的 `0..1` 音量包络，通过白名单 IPC 驱动 Live2D `ParamMouthOpenY`/`ParamMouthForm` 或 VRM `aa` expression；不得跨进程发送原始音频、波形采样或转写文本，中断、播放结束和窗口销毁时必须归零。

## 口型校准契约

- Live2D 优先读取 `.model3.json` 中 `Target=Parameter/Name=LipSync` 的参数组并驱动组内全部参数；参数组缺失或为空时尝试约定参数 `ParamMouthOpenY`。不存在的参数必须被运行时隔离，不能让渲染循环持续抛错。
- 每个桌宠模型独立保存 `gain/noiseGate/maxOpen/attack/release`，切换模型时恢复对应配置，未配置模型使用固定默认值，不得继承其他模型的校准结果。
- Realtime 与流式 PCM TTS 只跨 IPC 发送未经增益和门限处理的 `0..1` RMS 包络；PCM 播放器按 33ms 窗口预计算包络并以实际 `audio.currentTime` 对齐，不能把仅在控制面板渲染进程有效的 Blob URL 交给桌宠渲染进程。增益、噪声门、张嘴上限和开闭平滑统一在 Live2D/VRM 运行时应用，避免重复放大。
- 当前 TTS provider 不提供音素、viseme 或对齐时间戳，因此只能实现可靠的张嘴幅度同步，不能用文本时长猜测并宣称 A/I/U/E/O 精确口型；升级为 viseme 口型前必须先扩展 provider 输出契约。
- 可选 viseme 契约只接受 `sil/aa/ih/ou/ee/oh`、相对当前音频块的 `offset_ms`、可选 `duration_ms/weight`，并在 Python、Electron 主进程和渲染进程逐层限量、限幅和白名单校验。支持的 provider 可以返回 `{audio, visemes}`；不提供时间轴的 Genie 字节流必须继续使用 RMS，不做文本估时。
- 桌宠面板只展示“增益、噪声门、张嘴上限、开启速度、闭合速度、应用、恢复默认”等功能字段，不添加状态泡泡或调试说明。

## 体验评测契约

- 交互延迟按固定窗口聚合，只记录阶段耗时、成功次数和失败次数，不记录用户提示词、回复正文、音频或屏幕画面。窗口必须有上限，默认保留最近 200 个生成与 ASR 样本。
- 任务中心展示 `speech_end`、`asr_final`、`llm_request`、`llm_first_token`、`llm_first_sentence`、`tts_first_chunk`、`playback_start`、`interrupt_ack` 和麦克风到首播的 P50、P95 与样本数；即时语音另行展示连接、松开到首响应、松开到首播和中断确认。没有样本时显示“待采样”，不得显示为 0 ms。
- 麦克风到首播使用单调时钟连接 ASR 完成与播放器成功开始播放，中间包含前端交接、LLM、TTS、音频队列和播放启动等待；同一生成的流式分段快照只算一轮，避免重复放大样本。
- 本地 VAD 的静音设置表示安全上限，不是固定等待值；端点应根据语句长度缩短，并用同一会话的已恢复停顿统计避免抢话。设置、运行时和指标必须使用一致的 `160–1200ms` 支持范围。
- 自动语音打断必须在本地 VAD 连续确认约 96ms 后才触发，并复用统一的 generation/TTS/播放器取消链路；当前实现属于抗瞬态噪声的 VAD 确认，不得宣称已具备基于语义或回声转写的自适应 backchannel 判断。
- 打断命中率统计请求是否命中活跃生成，并必须分开记录语音自动打断与手动中断；工具成功率覆盖统一 ToolExecutor 的内置、插件与 MCP 工具结果。指标面板属于高级诊断能力，不在桌宠旁显示状态泡泡。
- 本地 TTS 打断必须保持推理互斥锁直到旧推理线程真实退出，并调用 provider 的停止能力；渲染端必须丢弃打断等待期及已确认取消 generation 的迟到音频，不能在清空播放器后再次开口。
- TTS 句段队列必须有硬上限并向 LLM 流施加背压；默认最多积压 3 个稳定句段。PCM 内存播放产生的 Blob URL 必须在播放结束、失败或打断时释放，不能用低延迟换取无界内存增长。
- 屏幕事实准确率、记忆召回精度、错误记忆率和单轮成本不能仅靠运行时计数推断，后续必须使用带标注的离线评测集、模型 usage 和价格版本数据实现，并明确区分实测与估算。
- 模型的 thinking/reasoning 策略由能力注册和场景路由单独管理。DeepSeek 在用户保持“默认思考”时沿用供应商默认行为，不因简单陪伴、问候或记忆查询而强制关闭 thinking；深度档、工作模式、联网搜索、工具任务和风险分析可以开启 thinking。用户显式选择始终优先于自动场景路由。

## 当前实现边界

- 即时、均衡、深度三档已经接入 Agent 路由。即时档语音在官方 OpenAI 凭据可用时使用原生 WebRTC speech-to-speech；不可用时自动回退流式 ASR、文本 Agent、句段 TTS。即时档文字输入以及均衡、深度档继续走文本 Agent 流水线。
- 均衡和深度语音链路的每轮麦克风首个非空音频块会旁路触发主 LLM 的 `/models` HTTP 预连接与 TTS 后台预热。LLM 预连接复用正式请求的连接池，具有 30 秒冷却和并发去重，不发送聊天补全、不产生生成 token；其作用是把 DNS/TLS/HTTP 建连尽量藏在用户说话期间，不等同于供应商原生 Realtime 或首 token 延迟保证。预连接状态只进入受保护的高级诊断接口，不在桌宠旁显示状态泡泡。
- partial ASR 只允许做非破坏性候选准备：本地排序最多 8 个工具名称并记录视觉候选帧 ID，缓存最多 32 项且 8 秒过期；`asr_final` 必须按文本相似度重新确认。候选只调整传给模型的工具定义顺序，不调用 handler、不跳过 ToolExecutor 权限检查、不写入记忆。普通语音不附加屏幕图像，只有最终文本明确提到屏幕、画面、窗口或同类视觉指代时才附加最新有效关键帧；手动文字对话保持原视觉上下文行为。
- 实时视觉采用两级门控：渲染端用 24x14 灰度签名和约 3.5% 归一化帧差减少重复上传，后端仍接收并替换每个通过门槛的最新短时帧，但独立视觉模型只在首帧、变化分数至少 12%、距上次分析至少 8 秒、距上次完成至少 30 秒或用户明确询问屏幕时运行。显式视觉请求强制分析当前帧并最多等待 900ms，超时后使用可证明的降级上下文，不无限阻塞主对话。
- 帧差仅是像素变化启发式，不等同于语义场景理解；独立视觉分析只接收单帧，因此 Prompt 禁止声称观察到前后变化、隐藏状态、操作完成态或不可读文字。截图确认回执提供采集原因、变化分数、分析状态、决策原因、尝试数和跳过数，供高级诊断与后续真实设备调参使用。
- 小变化帧复用最近一次视觉模型结论时保留原始 `observation_frame_id/analyzed_at/vision_model` 并标记 `vision_model_cached`；没有可追溯模型结论时只能返回 pending，不得把客户端 caption 当作可用视觉观察。
- 视觉分析完成、空结果或失败后必须用同一 `frame_id` 再次回传最终状态和分析耗时，不能让前端停留在 `pending`。高级体验指标只聚合视觉帧数、模型调用率、复用数、可用结论率、结果枚举与分析延迟 P50/P95，不保存截图、窗口标题、画面描述或 OCR 文本。
- 实时观察帧在 Electron 主进程中按原比例缩小到不超过 `1280x720`，并以质量 72 的 JPEG 发送；小图不得放大。该限制只服务低延迟桌面场景理解，OCR 和用户显式区域精确读取继续使用原始 PNG，不能用压缩观察帧代替精确读字输入。
- 本地 VAD 默认端点安全上限为 300ms，并按语句长度使用自适应基线；恢复成功的中途停顿会用于放宽后续端点，避免退化为固定等待。
- `sherpa-onnx-online` 已接入持久流式识别、设置保存和官方小型中文模型的受控安装；模型不纳入源码，可在首次启用服务或资源页手动勾选时下载并完成真实加载校验，真实麦克风 P50/P95 仍需在目标设备采样。
- 主 `agent:chat` 路径已按稳定句段流式进入 Genie-TTS；当 2.x provider 暴露 `tts_async()` 时，后端直接发送 `pcm_s16le/32kHz/mono` 二进制块，渲染端在内存中封装播放，不再落盘或经 HTTP 拉取 WAV；旧 provider 自动回退 WAV。当前粒度仍由 Genie 的句段推理决定，不是供应商原生语音模型的连续音频帧 Realtime。
- TTS 运行时能力注册表分别报告本地性、增量文本输入、音频流、传输格式、对齐层级、已观测 viseme、预热和取消语义；冷启动或客户端缺失时动态能力标为不可用，只有收到合法 viseme 时间线后才把对齐能力升级为 `viseme`。设置页仅显示这些功能字段与延迟指标，不开放未实现的 TTS 提供方。
- Genie TTS 的设置与 SVC 旁路面板只暴露当前 `tts()`/`tts_async()` 真正消费的字段；持久化、运行时和 API 使用同一字段集合，不接收已删除的提供方参数。
- 当前没有 Whisper provider，ASR 不保留 Whisper 模型、设备或计算精度字段；通用语言字段统一为 `language`。ASR 设置必须按 provider 只展示真实消费的服务地址、SenseVoice 或 Sherpa 参数；Sherpa ONNX 执行 provider 只接受上游支持的 `cpu/cuda/coreml`。
- Electron 模式使用同一个预加载控制面板承载麦克风、实时连接和快捷键事件；默认按住鼠标侧键 2 开始收音、松开后提交当前语音回合，用户可在设置页改用侧键 1，并录制“交互/拖动切换”“锁定切换”“打开陪伴面板”等全局键盘快捷键。即时档复用长连接 WebRTC 会话，其他档位发送本地 ASR 最终块。当前系统级侧键监听不屏蔽前台应用原有的后退/前进动作，不宣称独占输入。
- 模型能力注册表已登记当前 DeepSeek V4 与 Gemini 3 系列的官方元数据快照；其他提供商和未登记模型仍以“名称推断/未知”展示，不把推断价格或上下文当成事实。
- OCR 已按需加载并保留为精确读字备用能力；桌面实时观察继续使用短时图像帧和视觉模型。
- 延迟、打断、工具成功率已有运行时指标；屏幕事实准确率、错误记忆率和单轮成本仍需要独立评测集。

## 非目标

- 不是纯聊天网页 Demo。
- 不是以工作台或工程控制台作为主产品形态。
- 不是把所有模型栈直接塞进主 Python venv 的大一统环境。
- 不是要求用户先理解完整系统架构才能开始对话的工程控制台。
- 不是只靠提示词驱动桌宠动作的弱约束系统，后端必须做 schema、白名单和兜底校验。
